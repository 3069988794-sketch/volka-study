# Volka Study 项目规划书

> 拟定日期：2026-07-29
> 数据源：`3000 Textbook.pdf`（Volka English，364 页）
> 现状基线：`legacy/index.html`（单文件 demo）+ `scripts/extract_volka.py`
> 用法：本文档是执行清单。第 8 节按条推进，每条有验收标准，做完打勾。

**修订 v2（2026-07-29）——你提的两个问题带来的改动：**

| 改了什么 | 在哪 |
|---|---|
| 框架从 Vite+React+Hono 改成 **Next.js App Router**（前后端合成一个进程） | 3.2 / 3.3 |
| 数据库从 better-sqlite3 改成 **Node 内置 `node:sqlite`**（不装原生模块） | 3.2 / 3.3 |
| `sense_id` 从内容哈希改成 **永久 ID 注册表**（内容哈希有静默悬空 bug） | 3.4 / 4.2 |
| 音频与词级标注**不再预留、直接全量生成**，新增 M0.5，与 M1/M2 并行 | 3.5 / 8 |
| 音频移出 `public/`，改由 Route Handler 提供 | 3.2 / 3.6 |
| 待确认决策从 4 条降到 **1 条**（只剩每日时长） | 10 |

---

## 0. 目标

让一个中文母语的英语初学者，通过这份 PDF 的全部内容，达到**能在英语国家独立生活、工作，并与人自然交流**的水平。

翻译成可验证的标准：达到 **CEFR B2**——能就熟悉和不熟悉的话题流畅交谈，让对方不感到费力。这是绝大多数国家技术移民、大学入学和职场沟通的实际门槛。

---

## 1. 现实检查：这件事要多久

先把数字摊开，因为你原本的「45 天 × 45 分钟」和这个目标之间差了一个数量级，这个必须先说清楚。

### 1.1 CEFR 学时基准

行业普遍引用的指导学时（Cambridge English 等机构口径，累计值）：

| 目标等级 | 累计指导学时 |
|---|---|
| A1 | 约 90–100 小时 |
| A2 | 约 180–200 小时 |
| B1 | 约 350–400 小时 |
| **B2** | **约 500–600 小时** |

从零到 B2，约 500–600 小时。你原方案是 45 × 45 分钟 = **33.75 小时**，大约只够走完 A1 的三分之一。

### 1.2 这份 PDF 能覆盖多少

我把 PDF 重新抽了一遍（`pipeline/1_extract.py`，实测数见 4.1）：

| 指标 | 实测值 |
|---|---|
| 词条行 / 去重词形 | 3004 / 2998 |
| 义项 | **7223** |
| **带例句的义项** | **7223（100%）** |
| 独立例句 | 7220 |
| 例句长度 | 平均 7.9 词 / 中位数 8 / 最长 18 |

> 早期版本这张表写的是 2973 词形 / 6990 义项 / 6667 带例句，那是旧抽取脚本（`-layout` 版）的数，把一部分义项行当成续行吞掉了。上面是重建后的数。

按义项自身的 CEFR 等级分布：

| 等级 | 义项数 | 累计 |
|---|---|---|
| A1 | 1487 | 1487 |
| A2 | 1841 | 3328 |
| B1 | 1981 | 5309 |
| B2 | 1903 | 7212 |
| C1 / C2 | 3 / 8 | **7223** |

结论：**这份 PDF 的覆盖面正好卡在 B2**，词汇量（约 3000 词族）也正好对应「听懂 95% 日常口语」的研究阈值。**教材本身是够的**，不需要再找别的词表。真正的问题是它只是原料，不是训练方案。

### 1.3 教材给不了的四件事

知道 3000 个词 ≠ 能自然说话。中间的缺口是：

1. **产出能力** — 认得出 ≠ 说得出。教材只给识别材料。
2. **自动化程度** — 能想起来 ≠ 0.5 秒内想起来。对话不等人。
3. **发音与语调** — 说得对 ≠ 别人听得懂。
4. **互动策略** — 接话、打断、澄清、拖延（fillers）。这部分教材里其实有（`7.1 Politeness and Social Formulae`、`6.1 Interjections and Discourse Markers`），但埋在原书顺序深处。

这四点决定了整个应用的设计方向：**把教材从「识别材料」强制转化为「产出训练」**。7220 条平均 8 词的自然短句是完美的转化载体——短到能一口气跟读，长到包含真实语法结构。

### 1.4 应用管不了的部分（必须说清）

这个网站能承担约 **241 小时**（见 2.1 计算），剩下的 300 小时左右必须来自应用之外：

- **泛听** — 播客、剧集，每天 20–30 分钟（可与通勤/家务并行，不占应用时间）
- **泛读** — 分级读物 → 真实新闻
- **真人对话** — 语伴、线上外教，或用 Claude 语音对话兜底。**这一项无法替代**，第 4 阶段起每周至少 2 次。

应用的定位：**把词汇和句子练到能自动产出**。它是发动机，不是整辆车。

---

## 2. 学习方案：241 学习日 × 60 分钟

### 2.1 总量怎么算出来的

单个义项从第一次见到练成「能自动说出」的全生命周期成本：

| 环节 | 单次耗时 | 次数 | 小计 |
|---|---|---|---|
| 首次学习（看词形+音标+释义+听例句+跟读 2 遍） | 20s | 1 | 20s |
| FSRS 到期复习 | 8s | 8 | 64s |
| 产出训练（中→英口述） | 12s | 3 | 36s |
| **合计** | | | **120s = 2 分钟** |

7223 义项 × 2 分钟 = **14,446 分钟 ≈ 241 小时**。

按每天 60 分钟：**241 个学习日**。每周学 6 天休 1 天 → 日历跨度约 **281 天，9.2 个月**。

每天新学配额：7223 ÷ 241 ≈ **30 个新义项/天**。

其他强度档位（同样总量，自己选）：

| 每天 | 学习日 | 日历跨度 | 适合 |
|---|---|---|---|
| 40 分钟 | 361 天 | 约 13.8 个月 | 工作忙，追求不断更 |
| **60 分钟** | **241 天** | **约 9.2 个月** | **推荐（已定）** |
| 90 分钟 | 161 天 | 约 6.2 个月 | 全职备考，有大块时间 |

为什么推荐 60 分钟：低于 40 分钟，每日复习量会挤压掉产出训练，退化成纯背单词；高于 90 分钟，跟读会因口腔疲劳失去质量，而且难以长期维持 9 个月。

> 顺带一说：你原本直觉的「45 天」并没有错——它几乎正好是下面第 1 阶段的长度。你把一个阶段的时间当成了全程。

### 2.2 四个阶段

9.2 个月只有一个终点太反人性。切成四段，每段结束都是一个**能拿出去用的真实能力**，并配一次口语自测。

| 阶段 | 名称 | 等级 | 义项 | 学习日 | 完成后你能做到 |
|---|---|---|---|---|---|
| 1 | **落地生存** | A1 | 1487 | 50 | 问路、点餐、购物、说清时间数字金钱、礼貌应对陌生人 |
| 2 | **日常自理** | A2 | 1841 | 61 | 看病、租房、办手机卡、和邻居同事闲聊、描述感受和经历 |
| 3 | **工作胜任** | B1 | 1981 | 66 | 开会发言、写邮件、解释方案、电话沟通、处理工作分歧 |
| 4 | **自然交流** | B2+ | 1914 | 64 | 讨论抽象话题、表达细微态度、听懂玩笑和话外之意 |
| | | | **7223** | **241** | |

第 4 阶段的 1914 条 = B2 1903 + C1 3 + C2 8。那 11 条 C1/C2 是原书零星标注的，量太小不值得单独成段，并入 B2 尾部。

阶段之间安排 3 天缓冲（只复习不学新，做自测和复盘），所以日历上是 4 段 + 3 个缓冲期。

### 2.3 每日 60 分钟怎么分配

| 段 | 内容 | 时长 | 目的 |
|---|---|---|---|
| 1 | **热身跟读** — 昨天学的 10 句，纯跟读不评分 | 4 min | 零门槛开场，让嘴先动起来 |
| 2 | **到期复习** — FSRS 排期的卡，题型按轮次轮换 | 28 min | 主战场，趁脑子最清醒时做最费力的检索 |
| 3 | **新学** — 30 个新义项 | 10 min | 相对轻松，放在中段 |
| 4 | **产出口述** — 看中文说英文 + 录音 A/B 对比 | 15 min | 把识别转成产出，这一段决定你能不能开口 |
| 5 | **场景连读** — 今日义项串成一小段对话连续跟读 | 3 min | 有成就感的收尾 |

关键设计：**每段都有独立进度条，段与段之间有明确的过场**。开放式的「学到你想停」等于早退，明确的段落让人愿意做完。

### 2.4 每周结构

- **周一至周六**：标准 60 分钟会话
- **周日**：不学新内容。只做 ① 本周所有义项的一次快速跟读回顾（约 20 分钟）② 一次真人/AI 语音对话（20 分钟起）③ 看一次进度地图
- 周日的对话练习从第 1 阶段第 3 周就开始，不要等到「学好了再说」——那一天不会来

### 2.5 重排学习顺序：生存优先，不按原书顺序

这是整个方案里最有价值的一个改动。

原书 A1 的顺序是：`2.1 代词 → 2.6 否定 → 7.1 礼貌用语 → 1.1 人际关系 → 1.2 家居…`。按这个顺序学，你第 5 天在学「叔叔、阿姨、表哥」，但还不会说「多少钱」。

按「落地后最先需要什么」重排 66 个子分类号，分成 5 个优先级带：

**T0 — 头两周（活下去）**
`7.1 礼貌与社交套语` · `2.1 代词` · `2.6 否定` · `2.7 疑问词` · `5.8 基数词` · `5.6 时钟表达` · `1.10 数字金钱` · `1.8 地点与方向` · `1.3 食物与烹饪`
→ 两周后你能：问路、点餐、结账、说清时间和价格、礼貌拒绝

**T1 — 阶段 1 剩余（照顾自己）**
`1.4 购物与服务` · `1.2 家居日常` · `3.2 日常动作` · `1.7 交通出行` · `1.11 健康身体` · `5.2/5.3 月份星期` · `2.4 介词` · `2.5 助动词与情态动词`

**T2 — 阶段 2（和人来往）**
`6.1 语气词与话语标记` · `3.3 沟通` · `3.5 思考与感受` · `1.17 情绪与心理状态` · `4.10 心情` · `1.1 人际关系` · `1.9 时间与日程`

**T3 — 阶段 3（工作）**
`1.5 工作与商务` · `1.14 科技媒体网络` · `1.6 学校与学习` · `3.9 状态变化` · `4.21 连接词` · `2.8 从属连词`

**T4 — 阶段 4（抽象表达）**
`1.18 法律政务公共服务` · `1.16 社会文化社群` · `1.15 艺术娱乐体育` · `4.20 概率副词` · `4.12 一般描述`

实现方式：给子分类号打 `priority`，规划器按 `(阶段, priority, freq_rank)` 三级排序生成 241 天的每日清单，而不是按 PDF 页码。

原书只有 **66 个不同的子分类号**，它们在 A1/A2/B1/B2 四个 CEFR 大节里各出现一次，所以正文里一共出现 228 次（早期版本写「228 个子分类」，那是把出现次数当成了个数）。上面显式列出的 36 个号覆盖 T0–T4；没列到的号按义项自身等级落到 `LEVEL_BAND` 的默认带。实测分布：**T0 631 · T1 1491 · T2 1368 · T3 2051 · T4 1682**。

`priority` 只表达「同一阶段内谁更急着用上」，不用来把义项往后压——阶段本来就由义项自己的 CEFR 等级决定，所以 B2 的 `1.1 人际关系`（T2）不会因为带次靠前就跑到第一周去。

> 特别提醒 `6.1 语气词与话语标记`（well / actually / anyway / you know…）。这类词是「听起来像本地人」和「听起来像教科书」的分界线，原书排在很后面，方案里提到阶段 2 开头。

### 2.6 题型轮换：防枯燥的核心机制

同一个义项要过 8–11 遍。如果每遍都是「听 → 跟读」，第三周你就腻了。**同一义项在不同复习轮次用不同题型**，难度递增，从识别走到产出：

| 轮次 | 题型 | 形式 | 训练什么 |
|---|---|---|---|
| 1 | **首学** | 词形 + 音标 + 中文释义 + 听例句 + 跟读 2 遍 | 建立印象 |
| 2 | **听音辨义** | 听例句 → 4 选 1 选中文释义 | 识别 |
| 3 | **跟读评分** | 听原音 → 录音跟读 → A/B 对比回放 | 发音语调 |
| 4 | **盲听** | 不看文字听例句 → 自评听懂没 → 再看文字 | 听力解析 |
| 5 | **例句填空** | 挖掉目标词，从 4 个选项填 | 语境搭配 |
| 6 | **乱序重组** | 例句词序打乱，拖回正确顺序 | 语序直觉 |
| 7 | **中→英口述** | 给中文 + 首字母提示 → 说出英文 → 对比原句 | **产出** |
| 8+ | **中→英无提示** | 只给中文 → 说出英文 | **自动化** |

其中第 3 和第 7 是主力，出现频率最高。第 6「乱序重组」是唯一的鼠标拖拽题，其余全键盘操作。

### 2.7 从 PDF 里额外挖出来的东西

不需要任何外部数据源，从 6656 条例句里就能挖出来：

- **高频语块（chunks）** — 对全部例句做 2–5 gram 频次统计，会自动浮现 `I'd like to`、`Do you mind`、`How about`、`in the morning` 这类固定搭配。母语者是按语块说话的，不是按单词拼。这些语块单独成一个「口语砖块」模块，值得优先背熟。
- **场景对话** — 同一个子分类下的义项例句，本来就围绕同一主题，串起来天然是一段对话素材（第 5 段「场景连读」的内容来源）。
- **词形变化缺口** — 例句里出现的是 `went`、`children`、`better`，而词表里是 `go`、`child`、`good`。这个映射必须补（见 4.3），否则点词查不到。

---

## 3. 技术栈重新评估

### 3.1 我改变了上次的结论

上次我建议「保留单文件 vanilla HTML，只给 `serve.py` 加个接口」。**在你把「界面好看、交互流畅」列为坚持下去的首要条件之后，这个结论不成立了。**

理由是具体的，不是偏好问题：

- 现有 demo 24KB，但方案里需要 7 种题型、5 段会话引擎、223 天规划器、录音 A/B、进度图表、分级测试、深色模式、键盘系统。vanilla 实现会到 200KB+ 单文件。
- 更致命的是状态管理。`legacy/index.html:443-486` 每次 `renderCurrentLesson()` 都整块重绘并重新绑定所有监听器。在一个每 8 秒切一张卡、带录音状态和音频预取的应用里，这种模式必然出现监听器泄漏和状态错乱。
- 动效是「好看」的一半。手写 `requestAnimationFrame` 转场，一个卡片切换动画要几十行，而且做不到中断可逆。

**结论：上组件化框架。这是这个项目里唯一值得引入构建工具的地方。**

### 3.2 选定的栈（已按你的意见改为 Next.js）

| 层 | 选择 | 为什么 |
|---|---|---|
| 框架 | **Next.js App Router + TypeScript** | 前端、API、静态资源收进一个进程、一份配置、一条命令。TS 是刚性需求：demo 那个「用数组下标当进度 ID」的 bug 类型，类型系统会直接拦住 |
| API | **Route Handlers**（`app/api/*/route.ts`） | 不需要独立后端框架，不需要 dev 代理，API 与组件共用同一套类型定义 |
| 数据库 | **`node:sqlite`（Node 内置）** | Node 24 起内置，`DatabaseSync` 同步 API，单用户零并发问题。本机已是 **v24.14.1**。关键是**不装原生模块**：没有 node-gyp、没有 ABI 不匹配、Node 升级不用 rebuild、不需要 `serverExternalPackages` 配置 |
| 样式 | **Tailwind CSS v4** | 不用在 CSS 文件间跳转，配色/间距靠 token 统一 |
| 组件 | **shadcn/ui** | 代码复制进项目，你拥有它，不是黑盒依赖。质感直接到位 |
| 动效 | **Motion**（`motion` 包，原 framer-motion） | 声明式动画，可中断可逆，布局动画一行搞定 |
| 状态 | **Zustand** | 会话状态（当前段/当前卡/录音中）用它最省心，无样板代码 |
| 复习算法 | **ts-fsrs** | FSRS 比 SM-2 准，直接给出下次到期时间 |
| 图表 | **Recharts** | 进度仪表盘够用，别上 D3 |
| 手机访问 | **`next start -H 0.0.0.0`** + PWA manifest | 局域网直连，进度与电脑共用同一个库 |
| 数据管线 | **保留 Python** | `extract_volka.py` 已有 21KB 可用逻辑；PDF/NLP/TTS 生态在 Python 更强。只在离线跑一次，产出 JSON |

两种语言但边界干净：**Python 只做离线数据加工，TypeScript 做应用**，二者只通过 `data/senses.json` 通信。

**代价说清楚，别以后觉得被卖了东西：**

- 这个应用几乎每个组件都要 `"use client"`（音频、录音、键盘、Zustand 全是客户端状态）。Server Components 那套心智模型基本用不上，但约束还在，前几次会踩 hydration mismatch。Next 在这里的价值不在渲染模式，在于「一个进程搞定前端 + API + 静态资源」。
- HMR 比裸 Vite 略慢。Turbopack 之后差距不大，不会成为 8 个月里最烦人的那件事。
- 一个**未实测**的风险：Next 的 dev 文件监听会扫 `public/`，9600 个音频文件放进去可能拖慢启动。**对策：音频不放 `public/`，放 `data/audio/`，用一个 Route Handler 读文件返回（带 Range 和缓存头）。** 顺带把 300MB 从任何构建考虑里踢出去。

### 3.3 认真考虑过但否决的方案

| 方案 | 否决理由 |
|---|---|
| **保留 vanilla 单文件** | 见 3.1。唯一优点是零构建，但代价是 UI 目标做不到 |
| **Vite + React + Hono**（我上一版的方案） | 两个进程、两份配置、dev 要配代理、生产还得单独安排静态资源托管。Vite 的冷启动和 HMR 确实更快，但省下的那几秒换不回这些集成工作和长期维护面。**我上一版否决 Next 的理由（「SSR 零收益」）本身没错，但只算了没用上的功能是成本，没算它省掉的集成工作是收益——这是漏项，已改。** |
| **SvelteKit** | 客观上不比 React 差，内置 transition 甚至更适合这个场景，包体积更小。否决的唯一原因：React 的参考代码和社区答案多一个量级，遇到问题你（和我）解决得更快。选了 Next 之后这一条自动闭合 |
| **better-sqlite3** | 曾是我的默认选择，现在没必要了：它是原生模块，要 node-gyp 编译，Node 大版本升级要 rebuild，在 Next 里还得配 `serverExternalPackages` 才不被打进客户端包。`node:sqlite` 同样是同步 API，零依赖 |
| **Tauri 打包成 .exe** | 很诱人（真原生文件访问、双击即开、系统托盘提醒），但 v1 不做——Rust 工具链会拖慢前期迭代。**对策：把存储层抽象成 `lib/storage.ts` 接口，后期包 Tauri 只需换这一个文件的实现** |
| **Electron** | 200MB 起步的运行时，为一个学习应用不值得 |
| **纯 localStorage / IndexedDB** | 见 3.4，不满足你「重启不能丢」的强度要求 |

### 3.4 存储：这是你最在意的一条，单独设计

**当前 demo 的问题不是「会丢」那么简单，是「会静默错位」。**

`legacy/index.html:294` 的进度结构是 `{课序号: [项目下标]}`。它记的是「第 3 课第 17 项」。而你的 worklog 里还计划继续修提取器——**提取器一改，下标全部平移，进度就指到别的词上了。不会报错，只会让你看到一堆「学过」但其实没学的词。** 这比直接丢数据更糟。

另外 localStorage 绑在 origin 上：`127.0.0.1:8000` 换成 `:8001` 全没了；清一次浏览器数据全没了；没有备份，没有导出。

**设计：**

1. **唯一真相源是磁盘上的 SQLite 文件** `data/progress.db`。不是浏览器里的任何东西。
2. **稳定 ID：用注册表，不用内容哈希。** 我上一版写的是 `sense_id = sha1(headword|pos|gloss_en)[:12]`——**这个设计有缺陷，你问「为什么要预留」正好把它顶出来了。** M0 的任务清单里就写着「清理标题泄漏、修润中文释义」，一改 `gloss_en`，哈希就变，所有指向它的进度记录和词级标注全部悬空，而且不报错。这跟 demo 用数组下标当 ID 是**同一类** bug——我一边说那是全项目最危险的 bug，一边在 ID 设计里又犯了一次。
   **正确做法**：`data/id_registry.json` 一次性分配永久 ID，自然键（`headword|pos|该词内义项序号`）→ 不可变 ID。文本怎么改都不动 ID；新义项发新 ID；删掉的义项打墓碑、ID 不复用。`sense_id` 是不透明的永久标识，不是内容的函数。这个文件**不可再生成，必须和 `progress.db` 一起备份**。**这是整个项目的地基，M0 第一件事。**
3. **表结构**：
   - `sense_state(sense_id PK, stability, difficulty, due, reps, lapses, last_review, state)` — FSRS 状态
   - `review_log(id, sense_id, rating, card_type, elapsed_ms, ts)` — 每次答题的完整日志，可事后重算/换算法
   - `session(id, date, planned_min, actual_min, segments_done, new_count, review_count)`
   - `recording(sense_id, ts, blob_path)` — 跟读录音（可选保留）
   - `settings(key, value)` / `plan_day(day_no, stage, sense_ids_json)`
4. **写入策略**：每答完一张卡立即 `POST /api/review`，服务端同步落盘。单用户无并发。
5. **失败降级**：POST 失败则写入 localStorage 队列，恢复后自动补交。会话中断不丢答题记录。
6. **自动备份**：每天首次启动，把 `progress.db` 复制到 `data/backups/YYYY-MM-DD.db`，保留最近 30 份。8 个月的数据不能只有一份。
7. **一键导出**：设置页导出全部数据为 JSON。你的学习记录应该属于你，能带走。

### 3.5 音频：跟读的质量上限由它决定

现在 demo 用 `speechSynthesis` + `rate 0.66`。Windows 上的 Zira 音色机械感重，再降速到 0.66 会更明显。**你跟着一个机械音练 8 个月，练出来的就是机械音的语调。** 这一项必须换。

**方案：离线预生成 + 浏览器变速**

- 工具：[**edge-tts**](https://github.com/rany2/edge-tts)（Python 包，免 API key，用微软 Edge 的神经语音，当前 v7.2.8 活跃维护）
- 音色：`en-US-AriaNeural`（女）+ `en-US-GuyNeural`（男）双音色交替，避免单一音色听腻，也更接近真实听力环境
- 规模：6656 句 + 2955 词形 ≈ 9600 个片段，mp3 24kbps 单声道约 **300MB**，本地磁盘完全可接受
- 文件名：**例句文本自身的哈希**，不是 `sense_id`。这样改元数据、修释义都不用重跑；例句集变了也只补增量；批处理可断点续跑
- 位置：`data/audio/`，经 `app/api/audio/[hash]/route.ts` 提供（见 3.2 的说明，不放 `public/`）
- 生成时机：**M0 验收通过后立刻全量生成**（M0.5），与写 UI 并行，不等到 M4
- 慢速：**不额外生成文件**，用 `<audio>.playbackRate = 0.7`，Chrome 会保持音高
- 预取：播放当前卡时预加载下一张的音频，drilling 过程中不能出现任何加载等待
- 降级：某句音频缺失时自动回落 Web Speech，卡片不能因此卡死
- 风险与备选：edge-tts 走的是逆向出来的非官方接口，有失效可能。备选 ① [**Piper**](https://github.com/rhasspy/piper) 完全本地推理，永不失效，音质略逊 ② Azure Speech 付费，最稳

### 3.6 目录结构

```
D:\fqzl\volka-study\
├─ PLAN.md                  ← 本文档
├─ package.json  next.config.ts  tsconfig.json
├─ app\
│  ├─ layout.tsx  page.tsx        「今日」页
│  ├─ session\  journey\  progress\  browse\  placement\  settings\
│  └─ api\
│     ├─ review\route.ts          每张卡即时落盘
│     ├─ session\  state\  settings\  export\
│     └─ audio\[hash]\route.ts    读 data/audio/，带 Range 与缓存头
├─ components\
│  ├─ ui\                   shadcn/ui
│  ├─ cards\                8 种题型
│  └─ shadowing\            录音 + A/B 对比
├─ lib\
│  ├─ db.ts                 node:sqlite + 迁移 + 每日备份（server-only）
│  ├─ srs.ts                ts-fsrs 封装
│  ├─ audio.ts              播放 / 预取 / 降级
│  ├─ storage.ts            存储接口 ← 将来换 Tauri 只改这里
│  └─ types.ts              前后端共用的类型定义
├─ store\                   zustand
├─ pipeline\                Python，离线跑
│  ├─ 1_extract.py          PDF → senses.json
│  ├─ 2_enrich.py           音标 / 词形 / 语块
│  ├─ 3_annotate.py         LLM 词级义项标注
│  ├─ 4_tts.py              edge-tts 批量生成
│  └─ 5_validate.py         schema 校验 + 质量门禁
├─ data\
│  ├─ id_registry.json      ← 永久 ID 注册表，不可再生成，必须备份
│  ├─ senses.json           生成物（应用只读）
│  ├─ word_senses.json      词级标注（M0.5 产出）
│  ├─ plan.json             223 天规划
│  ├─ progress.db           ← 你的学习数据
│  ├─ audio\                9600 个 mp3
│  └─ backups\
└─ legacy\index.html        旧 demo 存档
```

启动方式：日常用 `npm start`（`next build` 后跑生产构建，最快），改代码时用 `npm run dev`。

---

## 4. 数据层

### 4.1 现有数据体检结果

**旧库 `data/volka-raw.json` 只剩两点价值：它的英文义项能当计数基线，它的 63 条中文能当种子。其余全部重做。**

实测（`pipeline/0_seed_cn.py` 跑出来的数）：旧库 6990 条义项里，`gloss_cn` 含中文的只有 **433 条**；这 433 条里又有 370 条是模板拼接的半成品——`……的人：writes or reports news stories`、`让……的东西：belongs to you`、`用于ask or offer help`。**3788 条 `gloss_cn` 干脆是英文原文照抄。**真正能用的只有 **63 条**（`I → 说话者，作动词主语`、`no one → 没有人`），几乎全在代词那几页。

我上一版写「`gloss_cn` 确实是带语境的人工级译文，这部分成果保留」，是只看了头几条就下的结论，错了。中文必须新造，见 M0 与 4.3。

旧库另外 5 处硬伤（`sub_section` 截断成 2 字符、323 条缺例句、5 条标题泄漏、漏采 C2、无 `example_cn`）在新管线里已不复存在，实测对比：

| 指标 | 旧库 | 新管线（`1_extract.py` + `2_enrich.py`） |
|---|---|---|
| 义项数 | 6990 | **7223** |
| 例句覆盖 | 6667（95.4%） | **7223（100%）** |
| 子分类名 | 46 个（截断） | **66 个完整名，跨 4 个阶段共出现 228 次** |
| C1/C2 义项 | 0 | **11**（C1 3 + C2 8） |
| 标题泄漏 | 5 条 | **0** |
| 页码混入例句 | 246 条 | **0** |
| 音标 / 语块 / 词频 | 无 | **7223 / 7223 / 7223** |
| 中文 | 63 条可用 | 待造 |

新管线的关键决定是**用 `pdftotext -raw` 而不是 `-layout`**。`-layout` 按版面坐标排序，水印把版面切碎后，某条义项的续行会被排到它自己的义项行之前（page 88 的 `ice".` 排在 `tooth` 之前，page 174 的 `city".` 排在那条 B2 义项之前），无法只凭位置判断归属，我为此写的三段启发式仍然错配了 3 条例句。`-raw` 按 PDF 内容流顺序输出，续行永远紧跟自己的义项行，那三段启发式全部删掉，串行残留从 3 条降到 0。

原书自身的三类缺陷在管线里显式处理，每一条都留痕可查：

| 缺陷 | 实例 | 处理 |
|---|---|---|
| 整段重排 | page 42–44 的 report/result/practice/homework/study/reading 共 16 条印了两遍 | `dedupe_repeated_runs` 删后一份，写入 `dropped_duplicate_runs` |
| 孤立鬼影行 | `century` 的义项行串进 `immediately` 中间；`earn` 的串进 `save` 中间 | `drop_ghost_lines` 按「释义+例句一字不差」判定，写入 `dropped_ghost_lines` |
| 原书印错 | page 106 `themselves` 的例句是 `planned the trip to .` | `pipeline/overrides.json` 按自然键手工修，每条附 `reason`，不影响 `sense_id` |

同释义但例句不同的 4 条（`garbage`、`billion`、`primary`、`colored`）是原书真给了两个例句，保留。

### 4.2 需要新增的字段

| 字段 | 来源 | 用途 | 状态 |
|---|---|---|---|
| `sense_id` | 注册表分配的永久不可变 ID（见 3.4 第 2 条） | **进度与词级标注的锚点，最高优先级** | ✅ 7223 条，两项门禁已过 |
| `lemma` | 去掉原书的同形词后缀与括注（`ring1`、`kind (caring)`） | 查音标、词频、屈折的基本形 | ✅ |
| `ipa` | CMUdict（`cmudict` 包），多词词条逐词查 | 首学卡显示音标 | ✅ 100% |
| `syllables` | CMUdict 元音数 | 跟读难度分档 | ✅ |
| `inflections` | `lemminflect` | 让例句里的 `went` 能查到 `go` | ✅ 6597 条（虚词无屈折） |
| `zipf` / `freq_rank` | `wordfreq` 的 Zipf 值，按词形排名 | 同优先级内的排序 | ✅ 100%，无零频词 |
| `chunk` | 例句中词形前后各两词，遇标点截断 | 跟读按语块停顿 | ✅ 100% |
| `priority` | 66 个子分类号按 2.5 的 T0–T4 打带，未列出的按义项自身 CEFR 落带 | 规划器排序 | ✅ T0 631 / T1 1491 / T2 1368 / T3 2051 / T4 1682 |
| `gloss_cn` | 63 条来自旧库种子，其余新造（带 `gloss_en` + 例句作语境约束） | 首学卡释义 | ⬜ 待造 7160 条 |
| `example_cn` | LLM 批量翻译（带上 `gloss_en` 做语境约束） | 中→英口述题 | ⬜ |
| `word_senses` | LLM 对每条例句做词级义项标注 | **点词得到语境内准确释义** | ⬜ M0.5 |
| `audio` | edge-tts 生成的文件名 | 播放 | ⬜ M0.5 |

### 4.3 「带语义的翻译」具体怎么实现

你要的是点句子里任意单词，给出**这句话里**的意思，不是词典里所有意思的拼接。

现在 demo 的做法（`legacy/index.html:527`）是把一个词所有义项的 `gloss_cn` 取前两条拼起来——点 `below` 得到「在…下方；在更低的位置」，不区分语境。而且 `legacy/index.html:531` 的 `{...dict, ...defaults}` 把一个 11 词的硬编码演示表覆盖在真实数据之上，必须删。

**正确做法：把语义在构建时定死，运行时只查表。**

对 6656 条例句跑一次 LLM 标注，每句输出：句中每个实词 → 它在这句里对应的 `sense_id` + 原形 + 该语境下的中文。存成静态 JSON。运行时零网络、零推理、完全离线，点击响应是查哈希表的速度。

顺带解决词形还原：标注时同时记录 `went → go`，比事后用规则猜准确得多。

这是 [VocabSieve](https://github.com/FreeLanguageTools/vocabsieve) 的核心思路——查词时把「单词 + 整句」一起交给查询，而不是只查孤立单词。

---

## 5. 前端与交互设计

你说得对——决定你能不能坚持 8 个月的，不是内容有多全，是每天打开它的那一刻愿不愿意开始。这一节按这个标准来设计。

### 5.1 十条设计原则

1. **打开就知道做什么。** 首页只有一个大按钮：`开始今天 · 第 37 天`。没有菜单选择，没有「今天学什么」的决策负担。决策疲劳是弃用的第一杀手。
2. **会话有明确的开始和结束。** 5 段流程，每段有独立进度条，最后有一张收尾卡（今天练了多少句/连续多少天/下次到期时间）。开放式的「学到你想停」等于早退。
3. **反馈必须在 100ms 内出现。** 按下评分键，卡片立刻响应——颜色、位移、声音任选，但不能等。
4. **进度要有地图，不要百分比。** 「你在第 2 阶段第 3 周，还有 11 天到日常自理」比「完成 34.7%」有力得多。223 天做成一条可视化路径，走过的点亮。
5. **连续天数要有宽容机制。** 连续 40 天后断一次就彻底放弃是真实存在的心理机制。设计：每周自动赠送 1 个「休息日额度」，用掉不算断更；最多累积 3 个。
6. **展示成长，不展示亏欠。** 显示「你现在能说 428 句英语」，不显示「你忘了 37 个词」。同一份数据，两种说法，留存率差很远。
7. **键盘驱动。** 每天 60 分钟、上百次交互，用鼠标点会累。全部核心操作走键盘（见 5.4）。这是 Anki 老用户快的原因。
8. **零加载感。** 预取下一张卡的音频。drilling 中任何一个 spinner 都会打断节奏。
9. **不能有死胡同。** 任何界面、任何状态，都有明确的下一步动作。
10. **尊重疲劳。** 深色模式（60 分钟长时段必需）、大字号例句、`prefers-reduced-motion` 支持。

### 5.2 页面清单

| 页面 | 内容 | 优先级 |
|---|---|---|
| **今日** | 唯一入口。大按钮 + 今日概况 + 连续天数 + 阶段进度条 | P0 |
| **会话** | 全屏沉浸，无导航栏，只有卡片 + 段落指示 + 进度条 | P0 |
| **旅程地图** | 223 天路径，4 阶段分段，当前位置高亮，阶段里程碑 | P1 |
| **能力面板** | 已掌握义项数 / 能说句数 / 8 主题掌握度雷达 / 日历热力图 / 遗忘曲线 | P1 |
| **词库** | 搜索浏览全部 6667 义项，可手动标记已会/重置 | P1 |
| **分级测试** | 首次进入时的 5 分钟定位（见 6.1） | P1 |
| **设置** | 每日时长、音色、主题、备份、导出 | P2 |
| **语块库** | 从例句挖出的高频语块 | P2 |

### 5.3 卡片布局

单列居中，最大宽度 680px。**drilling 时视线不应该左右移动**，所以不做多栏。

```
┌──────────────────────────────────────┐
│  段 2 / 5 · 到期复习      ▓▓▓▓▓░░░  │  ← 段落指示 + 进度
├──────────────────────────────────────┤
│                                      │
│            below                     │  ← 词形 32px
│          /bɪˈloʊ/  prep. A1          │  ← 音标 + 词性 + 等级
│                                      │
│   ┌────────────────────────────┐     │
│   │  ▶  The shoes are below    │     │  ← 例句 26px，可点词
│   │     the bed in my room.    │     │
│   └────────────────────────────┘     │
│                                      │
│        [T] 显示中文                   │  ← 默认隐藏，按需展开
│                                      │
│   ●  录音中… 0:02      [A] 对比回放   │
│                                      │
├──────────────────────────────────────┤
│  [1] 忘了  [2] 困难  [3] 良好  [4] 简单│  ← FSRS 四档
└──────────────────────────────────────┘
```

要点：
- **中文默认隐藏。** 先听先说，看中文是最后一步。这个默认值直接影响训练效果。
- **例句是可点的。** 点任意单词弹出该语境下的释义（4.3 的标注数据），弹窗不遮挡例句本身。
- **录音 A/B 对比是跟读的唯一反馈来源。** 你说只做跟读——纯跟读没有反馈会一直重复错误发音。录完立刻交替播放「原音 → 你的音 → 原音」，差异会自己暴露出来。这个纯前端实现，零成本，效果比想象中大。

### 5.4 键盘布局

| 键 | 动作 |
|---|---|
| `Space` | 播放 / 重播当前音频 |
| `S` | 慢速播放（0.7×） |
| `R` | 开始 / 停止录音 |
| `A` | A/B 对比回放（原音 ↔ 我的录音） |
| `T` | 显示 / 隐藏中文 |
| `1` `2` `3` `4` | FSRS 评分：忘了 / 困难 / 良好 / 简单 |
| `Enter` | 下一张 |
| `Esc` | 暂停会话（可安全退出，进度已落盘） |
| `?` | 快捷键帮助 |

设计约束：**手不用离开键盘左半区**（`1234`/`QWER`/`ASDF` 区域 + Space），右手可以完全离开鼠标。

### 5.5 视觉系统

**保留你 demo 的方向。** 那套暖纸色（`#f6f0e7` 底 / `#fffaf3` 面 / 墨绿 `#14532d` / 赭红 `#9a3412`）有辨识度，不是千篇一律的 SaaS 蓝紫渐变，而且暖色调对长时间阅读友好。这是 demo 里最值得保留的资产。

要做的是把它系统化：

- **提炼成 token**：`--bg / --surface / --ink / --muted / --line / --accent / --accent-warm / --success / --danger`，Tailwind v4 直接用 `@theme` 定义
- **补深色模式**：暖色系的深色版本用暖灰（`#1c1917` 系）而不是纯黑或冷灰，保持同一套体感
- **字号阶梯**：例句 26px / 词形 32px / 释义 16px / 元信息 12px。例句必须大到能扫视，跟读时眼睛不该费力
- **图表配色**：能力面板的图表走统一调色板，8 个主题需要 8 个可区分且在深浅两种模式下都可读的颜色（这块我有专门的调色规范可以套用，不要随手挑颜色）

### 5.6 动效原则

动效是「好看」的一半，但过度动效是「烦」的全部。

- **卡片切换**：150–200ms，只做 `opacity` + 轻微 `y` 位移。不做翻转、不做 3D
- **评分反馈**：<100ms 必须有视觉响应。四档用四种颜色的短暂描边脉冲
- **进度条**：300ms 缓动，让人看见它在动
- **段落过场**：段与段之间一个 500ms 的过场卡（「第 2 段完成 · 接下来是新学」），这是让人喘口气的地方，不是浪费时间
- **大动效留给稀缺时刻**：只有阶段完成、连续天数里程碑、当日会话完成才用庆祝级动效。稀缺才有价值——每张卡都放彩带，第三天就烦了
- **`prefers-reduced-motion`**：全部动效可关

---

## 6. 使用者视角的缺陷排查

以下是我把自己放在「一个初学者用 8 个月」的位置上，找出的会导致放弃的问题。按严重程度排。

### 6.1 首次打开不知道自己什么水平（严重）

**症状**：一个已有初中英语基础的人，被迫从 `I / you / he` 开始，第一天就觉得在浪费时间。
**对策**：首次进入做一个 **5 分钟分级测试**。从 6667 义项里按等级抽样，用二分法定位（答对往上、答错往下），约 40 题定出起点，已掌握的直接标记为「已会」跳过。规划器基于实际起点重算天数——可能你的 223 天变成 180 天，这本身就是激励。

### 6.2 复习积压雪崩（严重）

**症状**：断更 5 天回来，FSRS 甩出 400 张到期卡，看一眼就关掉，然后再也不打开。这是 Anki 用户弃用的头号原因。
**对策**：① 每日复习硬上限（比如 250 张），超出的按紧急度顺延；② 积压平滑：断更后把积压量分摊到未来 5–7 天，而不是一天清完；③ 回归时明确告知「你有 400 张积压，我已经分摊到未来 6 天，今天只需要做 80 张」。

### 6.3 今天只有 10 分钟怎么办（严重）

**症状**：知道今天做不完 60 分钟，于是干脆不做，然后断更，然后放弃。全有或全无是最危险的心态。
**对策**：提供三档模式，首页直接可选：
- **完整 60 分钟** — 标准流程
- **精简 20 分钟** — 只做最紧急的到期复习，跳过新学
- **保底 5 分钟** — 只跟读 10 句，纯粹为了保住连续天数

保底模式的存在本身就有价值：它让「今天做不完」不再等于「今天不做」。

### 6.4 纯跟读没有反馈等于自欺欺人（严重）

**症状**：你说只做跟读。但没有任何反馈的跟读，会让你带着错误发音重复 8 个月，而且自己完全不知道。
**对策**：录音 + 即时 A/B 对比（见 5.3）。零成本，纯前端。**这一条不是可选项。**
如果后期想要真正的发音纠正，再接 Azure 发音评测（音素级打分），但那需要后端和付费，v1 不做。

### 6.5 8 个月里总有不在电脑前的时候（中等）

**症状**：出差、周末在外，一断就是好几天。
**对策**：做成 PWA，服务器绑 `0.0.0.0`，手机连同一 WiFi 就能打开并装到桌面，进度共用同一个 `progress.db`。成本很低，价值很高。注意：iOS Safari 上录音有额外限制，手机端主要用于复习和听力，跟读仍以电脑为主。

### 6.6 数据丢失的恐惧本身会影响使用（中等）

**症状**：投入 8 个月后，任何数据风险都是心理负担。
**对策**：3.4 的自动备份 + 一键导出。**并且要让用户看得见**——设置页显示「上次备份：今天 09:14 · 共 30 份」。看不见的备份不能提供安全感。

### 6.7 重复内容的枯燥（中等）

**症状**：同一句话跟读 8 遍，第三周开始麻木。
**对策**：2.6 的 8 种题型轮换。同一义项每次复习换一种形式，难度递增。这是整个方案里防枯燥的主力机制。

### 6.8 不知道自己在进步（中等）

**症状**：第 60 天，感觉和第 30 天没区别（其实进步很大，只是不可见）。
**对策**：能力面板要给**绝对量**而不只是百分比：「能说 428 句」「掌握 1204 个义项」「覆盖日常英语约 61%」「8 主题掌握度雷达图」。另外每个阶段末的口语自测录音**保留下来**——第 4 阶段回听第 1 阶段的录音，是最强的进步证据。

### 6.9 其他

- **音频缺失时卡片不能死掉** — 必须降级到 Web Speech，不能白屏
- **会话中途关掉浏览器不能丢答题记录** — 每张卡即时落盘，不是会话结束才提交
- **不知道什么时候该停** — 段落进度 + 收尾卡解决
- **过度设计的风险** — 8 个月的项目如果 v1 要做全部功能，会永远上不了线。第 8 节的 M0–M3 是必须完成的最小闭环，M4 之后都可以边用边加

---

## 7. 现有 demo 的具体问题清单

保留：暖纸色视觉方向、`gloss_cn` 译文质量、Python 提取管线的基础逻辑、TTS 音色选择的思路、HTML 转义处理。

需要修的（文件位置基于归档后的 `legacy/index.html`）：

| # | 位置 | 问题 | 严重度 |
|---|---|---|---|
| 1 | `:294` `markDone()` | 进度用「课序号 + 项目下标」，数据一变就静默错位 | **致命** |
| 2 | `:239` `STORAGE_KEY` | localStorage 绑 origin，换端口/清缓存即丢，无备份 | **致命** |
| 3 | `:476` `speak(item.raw_text)` | 把整个词条（含词性、CEFR 等级、英文释义）丢给 TTS 朗读，跟读素材应该是单句例句 | 高 |
| 4 | `:527` `loadWordDict()` | 拼接一个词的所有义项，点词不分语境 | 高 |
| 5 | `:531` `{...dict, ...defaults}` | 11 词硬编码演示表覆盖真实数据 | 高 |
| 6 | `:270` `normalizeWord()` | 无词形还原，句中 `found` 查不到 `find` | 高 |
| 7 | `:443-486` `renderCurrentLesson()` | 每次整块重绘并重绑监听器，会泄漏 | 中 |
| 8 | 数据 | `sub_section` 截断成 2 字符，228 → 46 | 中 |
| 9 | 数据 | 323 条义项无例句 | 中 |
| 10 | 数据 | 5 条 `display_en` 混入标题；C2 义项漏采 | 中 |
| 11 | `:240` `SPEECH_RATE=0.66` | 固定慢速，且机械音；应改为预生成音频 + 可切换速度 | 中 |
| 12 | 全局 | 无键盘操作、无深色模式、无段落结构、无 45/223 天规划 | 中 |

---

## 8. 迭代路线

按顺序执行，每条做完打勾。**M0.5 是后台批处理，与 M1/M2 并行，不占串行时间。** M0–M3 是最小可用闭环，做完就能开始真正学习；M4 之后可以边用边加。

### M0 · 数据层重建

- [x] 建 `pipeline/` 目录，把 `scripts/extract_volka.py` 重构进 `1_extract.py`
- [x] **第一件事：建 `data/id_registry.json` 永久 ID 注册表**（自然键 → 不可变 ID，见 3.4 第 2 条）。之后所有产物只引用 ID，不引用文本 — 7223 条，`next_seq` 7224
- [x] 改用 `pdftotext -raw`，删掉 `-layout` 时代的三段串行启发式（见 4.1）
- [x] 修 `sub_section` 截断，恢复完整子分类名 — 66 个号，跨 4 阶段共 228 次出现
- [x] 例句覆盖率 — 100%（旧库缺的 323 条是旧提取的问题，PDF 里本来都有）
- [x] 清理标题泄漏（0 条）、页码混入（0 条），捞回 C1/C2 义项（11 条）
- [x] 原书自身缺陷显式处理：整段重排 16 条、鬼影行 2 条、印错 1 条，全部留痕（见 4.1）
- [x] `2_enrich.py`：`lemma`、`ipa`、`syllables`、`inflections`、`zipf`/`freq_rank`、`chunk` — 音标与语块 100%
- [x] 给子分类打 `priority`（按 2.5 的 T0–T4 分带）
- [x] **门禁：连跑管线两次 ID 完全一致（`minted_this_run: 0`）；改 `gloss_en` 后重跑，全库 0 条 ID 位移**
- [x] `0_seed_cn.py` 打捞旧库中文 — 已跑完，6990 条里只捞出 63 条可用（见 4.1）
- [ ] `3_translate_cn.py`：脚本已补，可合并 `cn_seed.json` 与断点缓存 `pipeline/_work/cn_translations.json`，并生成 73 个翻译批次；当前覆盖 **中文释义 63/7223、例句翻译 0/7223**。**7160 条中文释义 + 7223 条例句翻译要新造**，按 `(阶段, priority, freq_rank)` 切批，先补前几天用得到的
- [ ] `5_validate.py`：脚本已补，严格检查 schema、义项数、例句覆盖率、ID 唯一性、中文覆盖率、随机抽检 30 条；当前因 `data/senses.json` 未产出而正确失败
- [ ] 产出 `data/senses.json`

**验收**：`senses.json` 通过校验；义项数 ≥ 6990 ✅ 7223；例句覆盖率 ≥ 99% ✅ 100%；`sense_id` 全局唯一 ✅；子分类名完整 ✅；随机抽检 30 条无错；中文覆盖率 100%。**并且：连跑管线两次，ID 完全一致；手动改一条 `gloss_en` 后重跑，ID 不变。** ✅ 已验证。这一条是 M0.5 的放行门。

### M0.5 · 批量生成音频与词级标注（后台跑，与 M1/M2 并行）

这就是你问的「直接填」。这两件事本来排在 M4/M8，现在提到这里——它们是离线批处理，只依赖「例句集定稿 + ID 定稿」，不依赖任何 UI，而且早点有真音频真标注，M3 的跟读闭环才测得出来。

唯一的放行条件是 M0 验收通过（例句集不再变），因为例句一变音频要补、标注要重标。

- [ ] `4_tts.py`：先出 20 句样本定音色 → 再全量 9600 片段。文件名 = 例句文本哈希，与 `sense_id` 解耦；断点续跑，只补增量
- [ ] `3_annotate.py`：先标 100 句人工核对并调 prompt → 再全量 6656 句（每个实词 → 永久 `sense_id` + 原形 + 该语境下的中文）
- [ ] `example_cn` 批量生成（带 `gloss_en` 作语境约束）
- [ ] 产物校验：音频文件数与例句数对齐；标注里的每个 `sense_id` 都能在注册表里解析（零悬空引用）

**验收**：随机抽 20 句音频听感自然；随机抽 30 句标注，每个词的释义都符合该句语境；两份产物下游可直接消费，无需重跑。

**这一步不占我写代码的时间**：TTS 是网络批处理（约 30–80 分钟墙钟），标注是 LLM 批处理（约 130 个批次请求），都在后台跑，我同时做 M1/M2。

### M1 · 存储与服务端

- [ ] `npx create-next-app` + Tailwind v4 + TS
- [ ] `lib/db.ts`：`node:sqlite` 建表（`sense_state` / `review_log` / `session` / `settings` / `plan_day`）+ 迁移机制 + 每日首次启动自动备份，保留 30 份
- [ ] Route Handlers：`/api/review`、`/api/session`、`/api/state`、`/api/settings`、`/api/export`
- [ ] `lib/storage.ts`：存储接口抽象（将来换 Tauri 只改这里）
- [ ] 前端失败队列：POST 失败写 localStorage，恢复后补交

**验收**：答 10 张卡 → 关浏览器 → 换端口重开 → 进度完整。`data/backups/` 里有当天备份文件。断网状态下答题，恢复后记录自动补交。

### M2 · 应用骨架与视觉系统

- [ ] Next.js + TS + Tailwind v4 + shadcn/ui + Motion 初始化（M1 已建好工程，这里做视觉系统）
- [ ] 把 demo 的暖纸色提炼成 `@theme` token
- [ ] 深色模式（暖灰系）
- [ ] 布局与路由：今日 / 会话 / 地图 / 能力 / 词库 / 设置
- [ ] 「今日」页：一个大按钮 + 今日概况 + 连续天数 + 阶段进度
- [ ] 三档模式选择（60 / 20 / 5 分钟）
- [ ] `legacy/` 归档旧 demo

**验收**：`npm start` 一条命令起服务；深浅两种模式下所有页面可读；首页只有一个明确的主操作。

### M3 · 会话引擎与题型

- [ ] 5 段会话引擎（热身 / 复习 / 新学 / 产出 / 场景连读），每段独立进度
- [ ] `lib/srs.ts`：ts-fsrs 封装，四档评分
- [ ] 每日复习上限 + 积压平滑（6.2）
- [ ] 8 种题型组件（2.6），按复习轮次自动轮换
- [ ] 完整键盘布局（5.4）+ `?` 帮助浮层
- [ ] 卡片切换动效 + 段落过场 + 会话收尾卡
- [ ] 每张卡即时落盘

**验收**：能完整走完一次 60 分钟会话；全程不碰鼠标（除乱序重组题）；中途强制关闭浏览器后重开，已答记录不丢；连续 3 天使用后复习量分布合理，无雪崩。

### M4 · 音频与跟读（只剩前端部分）

音频文件在 M0.5 已经全量生成好了，这里只做播放与录音。

- [ ] `app/api/audio/[hash]/route.ts`：读 `data/audio/`，带 Range 与长缓存头
- [ ] `lib/audio.ts`：播放、慢速（`playbackRate 0.7`）、下一张预取、Web Speech 降级
- [ ] 录音（MediaRecorder）+ A/B 对比回放
- [ ] 跟读节奏控制：听 → 留白 → 再听

**验收**：连续答 50 张卡无一次加载等待；录音 A/B 能听出自己和原音的差异；删掉几个音频文件后自动降级不白屏。

### M5 · 规划器与旅程地图

- [ ] 规划器：按 `(阶段, priority, freq_rank)` 生成 223 天清单，写入 `plan_day`
- [ ] 支持按每日时长（40/60/90 分钟）重算总天数
- [ ] 旅程地图页：223 天可视化路径，4 阶段分段，当前位置高亮，里程碑标记
- [ ] 连续天数 + 休息日额度机制（每周赠 1，最多存 3）

**验收**：地图能看出「我在哪、还有多远、下一个里程碑是什么」；改每日时长后天数正确重算且已学进度不丢。

### M6 · 分级测试与自适应

- [ ] 5 分钟分级测试（二分法，约 40 题）
- [ ] 测试结果批量标记已掌握义项，规划器重算起点和总天数
- [ ] 设置页可重测

**验收**：一个有基础的人测完后总天数明显减少，且跳过的都是确实会的。

### M7 · 能力面板

- [ ] 绝对量指标：掌握义项数 / 能说句数 / 日常英语覆盖率
- [ ] 8 主题掌握度雷达图
- [ ] 日历热力图（含休息日区分）
- [ ] 遗忘曲线 / 每日答题量趋势
- [ ] 阶段末口语自测录音归档（可回听对比）

**验收**：连续用 2 周后，面板能直观显示成长；图表在深浅两种模式下都可读。

### M8 · 词级交互与移动端（标注数据 M0.5 已备好）

- [ ] 例句点词 → 语境内准确释义（消费 `word_senses.json`，替换 demo 的 `slice(0,2).join('；')` 拼接逻辑）
- [ ] 点词后可把该义项加入学习队列（遇到没学过的词时的自然动作）
- [ ] PWA manifest + `next start -H 0.0.0.0`，手机局域网访问
- [ ] 语块库页面（从例句挖出的高频搭配）

**验收**：随机点 30 个例句中的单词，释义都符合该句语境；手机能装到桌面并正常复习；进度与电脑共用。

### 之后（可选）

- Azure 发音评测（音素级打分）
- Claude API 场景对话陪练（限定只用已学词汇）
- Tauri 打包成 `.exe`（只需换 `storage.ts` 实现）
- B1/B2 之后的第二期教材

---

## 9. 开源参考

值得直接读代码或抄设计的项目：

| 项目 | 栈 | 值得看什么 |
|---|---|---|
| [**Lute v3**](https://github.com/LuteOrg/lute-v3) · [手册](https://luteorg.github.io/lute-manual/) | Python + Flask + SQLite | 和我们情况最像：本地自托管、点词查义、逐词记「已知/学习中/未知」状态。它的**词状态模型和进度存储方案可以直接借鉴** |
| [**LinguaCafe**](https://github.com/simjanos-dev/LinguaCafe) | Laravel + Vue，自托管 | 点词查义 + 事后复习的交互流程 |
| [**ts-fsrs**](https://github.com/open-spaced-repetition/ts-fsrs) · [文档](https://open-spaced-repetition.github.io/ts-fsrs/) · [完整 demo](https://github.com/ishiko732/ts-fsrs-demo) | TypeScript | 直接用作复习调度器 |
| [**VocabSieve**](https://github.com/FreeLanguageTools/vocabsieve) · [工作流文档](https://docs.freelanguagetools.org/workflows) | Python | 「查词时把单词 + 整句一起交给查询」的思路，以及词形还原的处理 |
| [**FSRS4Anki**](https://github.com/open-spaced-repetition/fsrs4anki) | — | FSRS 参数调优的实践经验 |
| [**edge-tts**](https://github.com/rany2/edge-tts) | Python | 免费神经语音批量生成 |
| [**Motion**](https://motion.dev/docs/react) | React | 动效实现参考 |

---

## 10. 待你确认的决策

原来列了 4 件，现在 4 件全部定了：

- ~~框架~~ → **定了：Next.js App Router**（你提的，我同意，理由见 3.2/3.3）
- ~~音频先不先做~~ → **定了：M0 一过就全量生成**，不用浏览器 TTS 顶着
- ~~词级标注要不要做~~ → **定了：M0 一过就全量标注**，和音频同批跑
- ~~每日时长~~ → **定了：60 分钟 × 241 学习日（约 9.2 个月）**，`plan.json` 按这个切天

**没有待确认项。** 下面三件不需要你决定、但我想让你知道我的处理方式：

- **旧 demo 会被移到 `legacy/`**，不删。它的视觉方向可以继承；它的中文译文不能——实测只有 63 条可用，见 4.1。
- **8.5 个月的时间跨度是真实的**，我没有为了让方案好看而压缩。你原来的 45 天直觉对应的是第 1 阶段（落地生存，46 天）——那个阶段结束时你确实已经能在国外点餐问路了，只是离「自然交流」还有三个阶段。
- **`data/id_registry.json` 是这个项目唯一不可再生成的文件**。`senses.json`、音频、标注全都能重跑，它不能。它和 `progress.db` 一起进备份。

---

## 附：核心数字速查

| 项 | 值 |
|---|---|
| 词条 / 去重词形 | 3004 / 2998 |
| 义项 / 带例句义项 | 7223 / 7223（100%） |
| 独立例句 | 7220（平均 7.9 词），语块平均 4.4 词 |
| 义项分布 | A1 1487 · A2 1841 · B1 1981 · B2 1903 · C1 3 · C2 8 |
| 子分类 | 66 个分类号 / 全书出现 228 次 |
| 优先级分布 | T0 631 · T1 1491 · T2 1368 · T3 2051 · T4 1682 |
| 单义项全周期成本 | 约 2 分钟 |
| 应用内总时长 | 约 241 小时 |
| 学习日 / 日历跨度 | 241 天 / 约 9.2 个月（60 分钟档，已定） |
| 每日新学 | 30 个义项 |
| 每日分配 | 热身 4 + 复习 28 + 新学 10 + 产出 15 + 连读 3 |
| 音频规模 | 约 10200 片段 / 约 320MB |
| 目标 | CEFR B2 |
















